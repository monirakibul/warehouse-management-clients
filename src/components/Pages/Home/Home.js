import React from 'react';
import Carousel from '../../Carousel/Carousel';

const Home = () => {
    return (
        <div className='flex-grow'>
            <Carousel></Carousel>
        </div>
    );
};

export default Home;